[package]
name = "dabqlite-sim"
description = "Deterministic simulation harness for dabqlite-core: simulated disk, crash injection, oracles"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
dabqlite-core = { path = "../dabqlite-core" }
# The one RNG (docs/DESIGN.md §7.1): ChaCha8 seeded from a u64, threaded
# through the simulator, never through the core.
rand = "0.8"
rand_chacha = "0.3"

[lints]
workspace = true
